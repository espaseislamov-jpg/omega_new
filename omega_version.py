"""Public desktop release identity."""
APP_NAME = 'Omega V3.0'
VERSION = '3.0.1'
# Bump this directory with every release that changes the bundled environment.
# The EXE must never load dependencies left by an older installer.
RUNTIME_DIR = '_runtime_v3_0_1_open'
